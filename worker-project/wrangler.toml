# File: wrangler.toml

# اسم الـ Worker الخاص بك
name = "orders-worker"

# الملف الرئيسي الذي سيبدأ منه الـ Worker
main = "index.js"

# تاريخ التوافق
compatibility_date = "2025-07-20"

# --- ✅ ربط قاعدة البيانات ---
[[d1_databases]]
binding = "DB"
database_name = "main_database"
database_id = "e0efb93b-1a2e-4131-b172-2c81985f9d02"
# --- ✅✅✅ ربط مخزن الملفات (R2 Bucket) ---
[[r2_buckets]]
binding = "RECEIPTS_BUCKET"  # هذا هو الاسم الذي يستخدمه الكود
bucket_name = "khadamatuk-receipts" # تم إضافة اسم المخزن الصحيح  
# ... (الإعدادات الموجودة مسبقًا تبقى كما هي)

# --- ✅✅✅ أضف هذا الجزء لتعريف المتغيرات ---
[vars]
R2_PUBLIC_URL = "https://pub-3744e16474fa47dba751a996a877f277.r2.dev" # 🚧 استبدل هذا بالرابط العام لمخزن R2 الخاص بك